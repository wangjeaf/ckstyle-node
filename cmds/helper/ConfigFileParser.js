exports.loadConfig = function(file, originConfig) {
    // 1. read file (ckstyle.json/ckstyle.ini/ckstyle-config.js)
        // 1.1 file exists, read it
        // 1.2 else read config from current dir
        // 1.3 else read config from user dir
    // 2. parse it
    // 3. replace some config of originConfig
    return originConfig
}